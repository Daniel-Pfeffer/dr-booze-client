export class DrinkPicker {
    cardHeader: string;
    picture: string;
    id: number;

    constructor(cardHeader: string, picture: string, id: number) {
        this.cardHeader = cardHeader;
        this.picture = picture;
        this.id = id;
    }
}
